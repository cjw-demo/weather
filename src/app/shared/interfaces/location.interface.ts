export interface CityInfo {
  city: string;
  country: string;
}

export interface GeoCodeInfo {
  name: string;
  local_names: any;
  lat: number;
  lon: number;
  country: string;
  state: string;
}
